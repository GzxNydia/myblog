<% include header.ejs %>
    <% docs.forEach(function (doc) {%>
        <p><h2><a href="#"><%= doc.title %></a></h2></p>
        <p class="info">
            作者：<a href="#"><%= doc.name %></a>
            日期：<a href="#"><%= doc.time %></a>
        </p>
        <p>
            <%- doc.content %>
        </p>
    <% }) %>
<% include footer.ejs %>
